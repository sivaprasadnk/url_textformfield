name: url_textformfield
description: Textformfield designed for inputing urls, with options to add leading and trailing text, appended at begining and end of input

version: 1.0.0
homepage: https://github.com/sivaprasadnk/url_textformfield

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: